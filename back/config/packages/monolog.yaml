monolog:

    handlers:
        main:
            type: stream
            path: "php://stdout"
            level: info
            formatter: monolog.formatter.line
            channels: ["!event", "!doctrine"]

        doctrine:
            type: stream
            path: "php://stdout"
            level: error
            channels: [ "doctrine" ]

        messenger:
            type: stream
            path: "php://stdout"
            level: debug
            channels: [ "messenger" ]
